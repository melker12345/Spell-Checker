{
  "name": "spell-checker-firefox",
  "version": "2.0.0",
  "description": "Fast and accurate spell checker for Firefox",
  "scripts": {
    "build": "browserify src/index.js -s nspell -o nspell-bundle.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "nspell": "^2.1.5"
  },
  "devDependencies": {
    "browserify": "^17.0.0"
  }
}
